import { KebabToTextPipe } from './kebab-to-text.pipe';

describe('KebabToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new KebabToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
