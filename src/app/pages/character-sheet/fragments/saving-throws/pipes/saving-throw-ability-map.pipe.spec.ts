import { SavingThrowAbilityMapPipe } from './saving-throw-ability-map.pipe';

describe('SavingThrowAbilityMapPipe', () => {
  it('create an instance', () => {
    const pipe = new SavingThrowAbilityMapPipe();
    expect(pipe).toBeTruthy();
  });
});
